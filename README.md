# ESP32C3 Zig Clay Example

Exploration of both integrating Zig code with the IDF backbone as well as using Clay to generate UI that is sent out via SPI to a display.

The code is initially based off of an IDF example to send an image via SPI to a display.

This WIP and doesn't work yet.

